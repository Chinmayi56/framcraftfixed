IMPORTANT: backend/.env was intentionally not included in the update ZIP because it may contain database/JWT/demo credentials from the original project.

Create backend/.env from backend/.env.example and restore your real deployment/local values before running the backend.
